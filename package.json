{
  "private": true,
  "scripts": {
    "serve": "concurrently --names 'tsc,zola' \"tsc --outdir public/scripts --watch\" \"zola serve\"",
    "build": "tsc && zola build",
    "ts": "tsc"
  },
  "prettier": {
    "printWidth": 140,
    "tabWidth": 2
  },
  "dependencies": {
    "concurrently": "^6.1.0",
    "typescript": "^4.2.4"
  }
}
