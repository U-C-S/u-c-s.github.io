{
  "private": true,
  "devDependencies": {
    "concurrently": "^6.1.0"
  },
  "scripts": {
    "serve": "concurrently --names 'tsc,zola' \"tsc --outdir public/scripts --watch\" \"zola serve\"",
    "build": "tsc && zola build",
    "ts": "tsc"
  },
  "prettier": {
    "printWidth": 140
  },
  "dependencies": {
    "typescript": "^4.2.4"
  }
}
