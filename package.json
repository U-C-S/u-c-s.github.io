{
  "private": true,
  "devDependencies": {
    "typescript": "^4.3.0-beta"
  },
  "scripts": {
    "serve": "concurrently --names 'tsc,zola' \"tsc --outdir public/scripts --watch\" \"zola serve\"",
    "build": "tsc && zola build",
    "ts": "tsc"
  },
  "prettier": {
    "printWidth": 140
  },
  "dependencies": {
    "concurrently": "^6.1.0"
  }
}
